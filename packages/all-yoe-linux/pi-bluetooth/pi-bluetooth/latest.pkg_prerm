#!/bin/sh
set -e
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl stop hciuart.service bthelper@.service

	systemctl $OPTS disable hciuart.service bthelper@.service
fi
