#!/bin/sh
set -e
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl daemon-reload
		systemctl preset "wcnss-start.service"

		if [ "enable" = "enable" ]; then
			systemctl --no-block restart "wcnss-start.service"
		fi
	fi
fi
