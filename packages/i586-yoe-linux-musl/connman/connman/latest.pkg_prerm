#!/bin/sh
set -e
	update-alternatives --remove  resolv-conf /etc/resolv-conf.connman
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl stop connman.service

	systemctl $OPTS disable connman.service
fi
