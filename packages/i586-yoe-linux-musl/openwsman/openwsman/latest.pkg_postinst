#!/bin/sh
set -e
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl daemon-reload
		systemctl preset "openwsmand.service"

		if [ "disable" = "enable" ]; then
			systemctl --no-block restart "openwsmand.service"
		fi
	fi
fi
