#!/bin/sh
set -e
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl daemon-reload
		systemctl preset "wired-connection.service"

		if [ "enable" = "enable" ]; then
			systemctl --no-block restart "wired-connection.service"
		fi
	fi
fi
