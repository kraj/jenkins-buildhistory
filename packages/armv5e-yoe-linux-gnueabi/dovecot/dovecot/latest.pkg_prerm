#!/bin/sh
set -e
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl stop dovecot.service dovecot.socket

	systemctl $OPTS disable dovecot.service dovecot.socket
fi
