#!/bin/sh
set -e
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl stop arno-iptables-firewall.service

	systemctl $OPTS disable arno-iptables-firewall.service
fi
