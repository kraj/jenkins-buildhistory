#!/bin/sh
set -e
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl daemon-reload
	systemctl preset ti-uim.service

	if [ "enable" = "enable" ]; then
		systemctl --no-block restart ti-uim.service
	fi
fi
