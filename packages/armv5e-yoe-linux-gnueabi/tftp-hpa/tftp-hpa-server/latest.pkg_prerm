#!/bin/sh
set -e
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl stop tftpd-hpa.socket tftpd-hpa.service

	systemctl $OPTS disable tftpd-hpa.socket tftpd-hpa.service
fi
