#!/bin/sh
set -e
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl stop tinyproxy.service

	systemctl $OPTS disable tinyproxy.service
fi
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl stop tinyproxy.service

	systemctl $OPTS disable tinyproxy.service
fi
