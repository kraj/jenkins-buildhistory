#!/bin/sh
set -e
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop "mdmonitor.service mdmon@.service"

		systemctl disable "mdmonitor.service mdmon@.service"
	fi
fi
