#!/bin/sh
set -e
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl stop xrdp.service xrdp-sesman.service

	systemctl $OPTS disable xrdp.service xrdp-sesman.service
fi
