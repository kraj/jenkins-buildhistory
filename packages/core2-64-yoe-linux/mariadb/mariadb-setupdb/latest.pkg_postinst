#!/bin/sh
set -e
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl daemon-reload
	systemctl preset install_db.service

	if [ "enable" = "enable" ]; then
		systemctl --no-block restart install_db.service
	fi
fi
