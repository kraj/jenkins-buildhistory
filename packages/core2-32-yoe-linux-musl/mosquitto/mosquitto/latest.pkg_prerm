#!/bin/sh
set -e
if [ -n "$D" -o ! -d /run/systemd/system ] && [ -z "$D" -a -x "/etc/init.d/mosquitto" ]; then
	/etc/init.d/mosquitto stop || :
fi
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop mosquitto.service

		systemctl disable mosquitto.service
	fi
fi
