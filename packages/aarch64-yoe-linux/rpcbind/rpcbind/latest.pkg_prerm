#!/bin/sh
set -e
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop rpcbind.service

		systemctl disable rpcbind.service
	fi
fi
if [ -n "$D" -o ! -d /run/systemd/system ] && [ -z "$D" -a -x "/etc/init.d/rpcbind" ]; then
	/etc/init.d/rpcbind stop || :
fi
