#!/bin/sh
set -e
if [ -n "$D" ]; then
	exit 0
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl stop logrotate.service logrotate.timer

	systemctl $OPTS disable logrotate.service logrotate.timer
fi
