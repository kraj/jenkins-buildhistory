#!/bin/sh
set -e
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop vsftpd.service

		systemctl disable vsftpd.service
	fi
fi
if [ -n "$D" -o ! -d /run/systemd/system ] && [ -z "$D" -a -x "/etc/init.d/vsftpd" ]; then
	/etc/init.d/vsftpd stop || :
fi
